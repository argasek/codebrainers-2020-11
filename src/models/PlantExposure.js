class PlantExposure {
  /**
   * @constructor
   * @param {string} id
   * @param {string} name
   * @param {string} emoji
   */
  constructor(id = '', name = '', emoji = '') {
    this.id = id;
    this.name = name;
    this.emoji = emoji;
  }
}

export default PlantExposure;